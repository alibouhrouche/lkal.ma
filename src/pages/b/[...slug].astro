---
import App from "@/app";
import Layout from "@/layouts/Layout.astro";
export async function getStaticPaths() {
  return [{ params: { slug: undefined } }];
}
---

<Layout>
  <App ssrPath="/b" client:load />
</Layout>
